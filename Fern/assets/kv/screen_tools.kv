<ToolsScreen>
    name:'tools'
    MDIconButton:
        icon:'menu'
        on_press: app.navbar.set_state("open")
        pos_hint: { 'center_x': .05, 'center_y': .925 }
    MDLabel:
        text:'Ferramentas'
        font_style: "H4"
        pos_hint:{'center_x': .5, 'center_y': .925 }
        halign: 'center'

    MDSeparator:
        size_hint_x: .9
        pos_hint:{'center_x': .5, 'center_y': .875 }

    AutoSwitch:
        pos_hint:{'center_x': .5, 'center_y': .7 }

    Status:
        pos_hint:{'center_x': .25, 'center_y': .35 }

    Status:
        pos_hint:{'center_x': .75, 'center_y': .35 }


<Status@MDCard>
    size_hint: .4, .45
    padding: [7, 2, 2, 10 ]
    spacing: 2
    orientation: 'vertical'
    radius:[10,]
    #md_bg_color:[1,1,1,.5]
    MDLabel:
        text: app.SAGE_1.name.replace('_', ' ')
        font_style:'H5'
        halign:'center'
    MDSeparator:
        color: app.theme_cls.primary_color
        pos_hint:{'center_x': .5}
        size_hint_x: .8
    ConnectionState:
        target: app.SAGE_1
        pos_hint:{'center_x': .5}
        size_hint_x: .8  
    MDSeparator:
        # color: app.theme_cls.primary_color
        pos_hint:{'center_x': .5}
        size_hint_x: .8 
    MDBoxLayout:
        padding: [1, 7.5, 1, 7.5]
        size_hint_x: .8
        adaptive_height: True
        pos_hint:{'center_x': .5}
        MDLabel:
            text: 'GCD'
            adaptive_height: True
            # md_bg_color: [ 1, 0, 1, .5 ]
        MDLabel:
            text:  'desativado'
            halign:'right'
            adaptive_height: True
            # md_bg_color: [ 1, 1, 0, .5 ]
    MDBoxLayout:
        padding: [1, 7.5, 1, 7.5]
        # md_bg_color: [1, 1, 1, .5]
        size_hint_x: .8
        adaptive_height: True
        pos_hint:{'center_x': .5}
        MDLabel:
            text: 'Servidor'
            adaptive_height: True
            # md_bg_color: [ 1, 0, 0, .5 ]
        MDLabel:
            text:  'stand by'
            halign:'right'
            adaptive_height: True
            # md_bg_color: [ 0, 1, 0, .5 ]
    MDBoxLayout:
        padding: [1, 7.5, 1, 12.5]
        size_hint_x: .8
        adaptive_height: True
        pos_hint:{'center_x': .5}
        MDLabel:
            text: 'VisorAcesso'
            adaptive_height: True
            # md_bg_color: [ 0, 1, 1, .5 ]
        MDLabel:
            text:  'Aberto'
            halign:'right'
            adaptive_height: True
            # md_bg_color: [ 0, 0, 1, .5 ]
    
    MDRoundFlatButton:
        text:'Dashboard'
        pos_hint:{'center_x': .5}
